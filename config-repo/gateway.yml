server:
  port: 8060

management:
  security:
    enabled: false

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000

zuul:
  ignoredServices: '*'
  routes:
    app-service:
      path: /svc/**
      sensitiveHeaders: